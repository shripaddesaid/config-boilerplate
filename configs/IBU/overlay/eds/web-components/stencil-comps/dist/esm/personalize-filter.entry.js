import { r as registerInstance, c as createEvent, h } from './index-DClFOSUP.js';
import { a as SlidersHorizontalIcon } from './Svg-DC38Oo0s.js';
import { M as MOBILE_BREAKPOINT, C as CONTAINER_WIDTH } from './breakpoints-xVQ0t8-X.js';

const personalizeFilterCss = ".filter-header{display:flex;width:100%;padding:var(--lds-g-spacing-250, 20px) 0;align-items:flex-start;justify-content:space-between}.personalize-filter-container{display:flex;flex-direction:column;width:100%;align-items:flex-start}.personalize-mobile-sheet{display:flex;flex-direction:column;width:100%;height:100%}.filter-caption{display:flex;align-items:center;gap:var(--lds-g-spacing-200, 16px)}.filter-icon{display:flex;width:24px;height:24px;min-width:24px;max-width:24px;min-height:24px;max-height:24px;flex-direction:column;justify-content:center;align-items:center;aspect-ratio:1/1}.filter-svg{stroke:var(--lds-g-color-neutral-base-100, #111111)}.filter-svg path:first-child,.filter-svg path:nth-child(2){stroke:var(--lds-g-color-neutral-base-100, #111111)}.filter-svg path:nth-child(3),.filter-svg path:nth-child(4),.filter-svg path:nth-child(5),.filter-svg path:nth-child(6){stroke:var(--lds-g-color-neutral-base-100, #191919)}.filter-title{flex:1 0 0;color:var(--lds-g-color-neutral-base-100, #000);font-family:var(--lds-g-font-family-sans-serif, Ringside);font-size:var(--lds-g-font-size-3, 24px);font-style:normal;font-weight:var(--lds-g-font-style-serif-regular);line-height:var(--lds-g-font-line-height-9, 2rem);margin:0}.clear-filters{display:flex;flex-direction:column;justify-content:center;flex-shrink:0;align-self:stretch;color:var(--lds-g-color-neutral-base-100, #191919);text-align:right;font-family:var(--lds-g-font-family-sans-serif, Ringside);font-size:var(--lds-g-font-size-base, 16px);font-style:normal;font-weight:900;line-height:var(--lds-g-font-line-height-5, 1.5rem);text-decoration-line:underline;text-decoration-style:solid;text-decoration-skip-ink:none;text-decoration-thickness:4%;text-underline-offset:24%;text-underline-position:from-font;cursor:pointer;margin:0}.clear-filters--disabled{display:flex;flex-direction:column;justify-content:center;flex-shrink:0;align-self:stretch;color:var(--lds-g-color-disabled-1);cursor:not-allowed;text-align:right;font-family:var(--lds-g-font-family-sans-serif, Ringside);font-size:var(--lds-g-font-size-base, 16px);font-style:normal;font-weight:900;line-height:var(--lds-g-font-line-height-5, 1.5rem);text-decoration-line:underline;text-decoration-style:solid;text-decoration-skip-ink:none;text-decoration-thickness:4%;text-underline-offset:24%;text-underline-position:from-font;margin:0}.filter-accordion-block{width:100%;display:flex;flex-direction:column}.filter-options-list{padding-bottom:var(--lds-g-spacing-200)}.selected-chips{display:flex;padding-bottom:var(--lds-g-spacing-200);align-items:flex-start;gap:var(--lds-g-spacing-050);flex-wrap:nowrap;width:100%;box-sizing:border-box}.close-icon-container{width:var(--lds-g-spacing-300, 24px);height:var(--lds-g-spacing-300, 24px);flex-shrink:0}.close-icon-container svg{fill:var(--lds-g-color-neutral-base-100, #191919)}.icon-header{display:flex;justify-content:flex-end;padding-right:var(--lds-g-spacing-200, 16px)}.personalize-title{color:var(--lds-g-color-neutral-base-100, #000);text-align:center;align-self:stretch;font-family:var(--lds-g-font-family-sans-serif);font-size:var(--lds-g-font-size-2, 20px);font-style:normal;font-weight:var(--lds-g-font-style-serif-regular);line-height:var(--lds-g-font-line-height-6, 1.625rem)}.container-body{display:flex;padding:var(--lds-g-spacing-0) var(--lds-g-spacing-200) var(--lds-g-spacing-300) var(--lds-g-spacing-200);flex-direction:column;align-items:flex-start;gap:var(--lds-g-spacing-300);flex:1 0 0;align-self:stretch;border-bottom:1px solid var(--lds-g-color-border-3);overflow-y:auto}@media (min-width: 375px){.container-body{padding:var(--lds-g-spacing-0) var(--lds-g-spacing-400) var(--lds-g-spacing-300) var(--lds-g-spacing-400)}}.mobile-footer{display:flex;height:var(--lds-g-spacing-1400, 112px);padding:0 var(--lds-g-spacing-250);align-items:center;gap:var(--lds-g-spacing-200, 16px);align-self:stretch}.clear-filters__mobile{display:flex;flex-direction:column;justify-content:center;flex:1 0 0;align-self:stretch;color:var(--lds-g-color-neutral-base-100);font-family:var(--lds-g-font-family-sans-serif, Ringside);font-size:var(--lds-g-font-size-base, 16px);font-style:normal;font-weight:900;line-height:var(--lds-g-font-line-height-5, 1.5rem);text-decoration-line:underline;text-decoration-style:solid;text-decoration-skip-ink:none;text-decoration-thickness:4%;text-underline-offset:24%;text-underline-position:from-font;cursor:pointer}.clr-filter-btn{display:flex;justify-content:center;align-items:center;gap:var(--lds-g-spacing-150)}.clear-filters__mobile--disabled{color:var(--lds-g-color-disabled-1);cursor:not-allowed;font-family:var(--lds-g-font-family-sans-serif, Ringside);font-size:var(--lds-g-font-size-base, 16px);font-style:normal;font-weight:900;line-height:var(--lds-g-font-line-height-5, 1.5rem);text-decoration-line:underline;text-decoration-style:solid;text-decoration-skip-ink:none;text-decoration-thickness:4%;text-underline-offset:24%;text-underline-position:from-font}.mobile-section{width:100%;display:flex;flex-direction:column;align-items:stretch;margin-bottom:var(--lds-g-spacing-300, 24px)}.mobile-section-header{display:flex;flex-direction:column;width:100%;margin-bottom:var(--lds-g-spacing-200, 16px)}.mobile-section-label{width:100%;color:var(--lds-g-color-palette-stone-100, #555);font-family:var(--lds-g-font-family-sans-serif, Ringside, sans-serif);font-size:var(--lds-g-font-size-1, 18px);font-weight:var(--lds-g-font-style-serif-regular);line-height:var(--lds-g-font-line-height-7, 1.75rem);padding:var(--lds-g-spacing-150, 12px) 0;border-bottom:1px solid var(--lds-g-color-border-3, #E0E0E0)}.chip-row{display:flex;flex-wrap:wrap;gap:var(--lds-g-spacing-150, 12px);padding:0 var(--lds-g-spacing-050, 4px);align-items:flex-start;align-self:stretch}.visually-hidden{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0, 0, 0, 0);white-space:nowrap;border:0}";

const PersonalizeFilter = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.applyFilters = createEvent(this, "applyFilters");
        this.rollbackFilters = createEvent(this, "rollbackFilters");
        this.requestClose = createEvent(this, "requestClose");
        this.filterChange = createEvent(this, "filterChange");
    }
    filterHeader;
    clearFilterTitle;
    resultButtonTemplate;
    noResultFoundButtonTitle;
    resultCount = 0;
    alwaysMobile = false; // Stay in mobile mode (for sheet usage)
    filters = [];
    selectedFilters = {}; // groupId -> selected option values
    initialSnapshot = null;
    selectedChipsEl;
    resizeObserver;
    chipRefs = new Map();
    overflowChipProbeEl;
    chipWidthCache = new Map();
    overflowChipWidth = 50; // fallback, smallest possible count chip width
    applyFilters;
    rollbackFilters;
    openGroup = null; // track which filter group is open
    isMobile = typeof window !== 'undefined' ? window.innerWidth <= MOBILE_BREAKPOINT : false;
    requestClose;
    filterChange;
    selectedChipContainerWidth = 0;
    visibleChips = {};
    hiddenChipCounts = {};
    componentWillLoad() {
        this.updateIsMobile();
        if (Array.isArray(this.filters) && Object.keys(this.selectedFilters).length === 0) {
            const obj = {};
            this.filters.forEach(f => { obj[f.id] = []; });
            this.selectedFilters = obj;
        }
    }
    async startSession() {
        this.initialSnapshot = JSON.parse(JSON.stringify(this.selectedFilters || {}));
    }
    async rollbackToInitial() {
        if (this.initialSnapshot) {
            this.selectedFilters = JSON.parse(JSON.stringify(this.initialSnapshot));
            this.filterChange.emit(this.selectedFilters);
            this.requestClose.emit();
            this.rollbackFilters.emit(this.selectedFilters);
            this.initialSnapshot = null;
        }
        else {
            this.requestClose.emit();
        }
    }
    async commitAndClose() {
        this.applyFilters.emit(this.selectedFilters);
        this.initialSnapshot = null;
        this.requestClose.emit();
    }
    componentDidLoad() {
        window.addEventListener('resize', this.handleResize);
    }
    _currentProbeOverflowValue() {
        return Math.max(0, ...Object.values(this.hiddenChipCounts));
    }
    updateVisibleChips = () => {
        if (!this.selectedChipsEl) {
            return;
        }
        requestAnimationFrame(() => {
            this.chipRefs.forEach((el, key) => {
                const width = el.getBoundingClientRect().width;
                if (width > 0) {
                    this.chipWidthCache.set(key, width);
                }
            });
            requestAnimationFrame(() => {
                const updates = {};
                const hiddenUpdates = {};
                this.filters.forEach(group => {
                    const result = this.getVisibleChipsForGroup(group.id);
                    if (Array.isArray(result)) {
                        const visible = result;
                        const hidden = (this.selectedFilters[group.id] || []).length - visible.length;
                        if (JSON.stringify(this.visibleChips[group.id]) !== JSON.stringify(visible) ||
                            this.hiddenChipCounts[group.id] !== hidden) {
                            updates[group.id] = visible;
                            hiddenUpdates[group.id] = hidden;
                        }
                    }
                });
                if (Object.keys(updates).length > 0) {
                    this.visibleChips = { ...this.visibleChips, ...updates };
                    this.hiddenChipCounts = { ...this.hiddenChipCounts, ...hiddenUpdates };
                }
            });
        });
    };
    resizeHandler = (entries) => {
        for (const entry of entries) {
            const w = entry.contentRect.width;
            if (this.selectedChipContainerWidth !== w) {
                this.selectedChipContainerWidth = w;
                this.updateVisibleChips();
            }
        }
    };
    componentDidRender() {
        if (this.resizeObserver) {
            this.resizeObserver.disconnect();
            this.resizeObserver = undefined;
        }
        if (this.selectedChipsEl) {
            this.resizeObserver = new ResizeObserver(this.resizeHandler);
            this.resizeObserver.observe(this.selectedChipsEl);
            this.updateVisibleChips();
        }
    }
    disconnectedCallback() {
        window.removeEventListener('resize', this.handleResize);
        if (this.resizeObserver) {
            this.resizeObserver.disconnect();
            this.resizeObserver = undefined;
        }
        this.chipWidthCache.clear();
    }
    getVisibleChipsForGroup(groupId) {
        if (!this.selectedChipsEl) {
            return undefined;
        }
        const containerWidth = this.selectedChipsEl.clientWidth;
        const selected = this.selectedFilters[groupId] || [];
        const gap = 4; // gap between chips in px as per figma
        if (this.overflowChipProbeEl) {
            const chip = this.overflowChipProbeEl.querySelector('chip-component');
            if (chip) {
                const width = chip.getBoundingClientRect().width;
                if (width && width !== this.overflowChipWidth) {
                    this.overflowChipWidth = width;
                }
            }
        }
        const overflowChipWidth = this.overflowChipWidth;
        const visible = [];
        let usedWidth = 0;
        for (let i = 0; i < selected.length; i++) {
            const sel = selected[i];
            const chipEl = this.chipRefs.get(sel);
            const cachedWidth = this.chipWidthCache.get(sel);
            const measuredWidth = chipEl ? chipEl.getBoundingClientRect().width : 161; // smallest possible chip width
            const chipWidth = cachedWidth || measuredWidth;
            const overflow = selected.length - (i + 1) > 0;
            let projectedWidth = usedWidth;
            projectedWidth += chipWidth + (visible.length > 0 ? gap : 0);
            if (overflow) {
                projectedWidth += overflowChipWidth + gap;
            }
            if ((visible.length === 0 && projectedWidth - (overflow ? (overflowChipWidth + gap) : 0) <= containerWidth) ||
                projectedWidth <= containerWidth) {
                visible.push(sel);
                usedWidth += chipWidth + (visible.length > 1 ? gap : 0);
            }
            else {
                break;
            }
        }
        return visible;
    }
    handleResize = () => {
        this.updateIsMobile();
    };
    updateIsMobile = () => {
        if (this.alwaysMobile) {
            this.isMobile = true;
            return;
        }
        const mobile = window.innerWidth <= MOBILE_BREAKPOINT;
        if (mobile !== this.isMobile) {
            this.isMobile = mobile;
        }
    };
    toggleOpenGroup = (groupId) => {
        this.openGroup = (this.openGroup === groupId) ? null : groupId;
    };
    handleOptionCheck = (groupId, value) => {
        const current = this.selectedFilters[groupId] || [];
        const set = new Set(current);
        set.has(value) ? set.delete(value) : set.add(value);
        this.selectedFilters = {
            ...this.selectedFilters,
            [groupId]: Array.from(set),
        };
        // Emit change after user action
        this.filterChange.emit(this.selectedFilters);
    };
    handleChipClose = (groupId, value) => {
        this.selectedFilters = {
            ...this.selectedFilters,
            [groupId]: this.selectedFilters[groupId].filter(v => v !== value),
        };
        // Emit change after user action
        this.filterChange.emit(this.selectedFilters);
    };
    clearFilters = () => {
        const cleared = {};
        this.filters.forEach(group => { cleared[group.id] = []; });
        this.selectedFilters = cleared;
        // Emit change after user action
        this.filterChange.emit(this.selectedFilters);
    };
    isClearFilterDisabled() {
        return !Object.values(this.selectedFilters).some(arr => arr.length > 0);
    }
    // Accessibility =>
    handleKeyDownClose = (event) => {
        if (event.key === 'Enter' || event.key === ' ') {
            event.preventDefault();
            this.requestClose.emit();
        }
    };
    handleKeyDownClearFilters = (event) => {
        if (event.key === 'Enter' || event.key === ' ') {
            event.preventDefault();
            if (!this.isClearFilterDisabled())
                this.clearFilters();
        }
    };
    handleCheckboxKeyDown = (event, groupId, value) => {
        if (event.key === 'Enter' || event.key === ' ') {
            event.preventDefault();
            event.stopPropagation();
            this.handleOptionCheck(groupId, value);
        }
    };
    handleChipKeyDown = (event, groupId, value) => {
        if (event.key === 'Enter' || event.key === ' ') {
            event.preventDefault();
            event.stopPropagation();
            this.handleChipClose(groupId, value);
        }
    };
    render() {
        if (this.isMobile) {
            return (h("div", { class: "personalize-mobile-sheet" }, h("div", { class: "icon-header" }, h("div", { class: "close-icon-container", id: "closeIcon", tabIndex: 0, "aria-label": "Close", onClick: () => { this.rollbackToInitial(); }, onKeyDown: this.handleKeyDownClose }, h("svg", { xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16", fill: "none" }, h("path", { d: "M15.281 14.2194C15.3507 14.2891 15.406 14.3718 15.4437 14.4629C15.4814 14.5539 15.5008 14.6515 15.5008 14.7501C15.5008 14.8486 15.4814 14.9462 15.4437 15.0372C15.406 15.1283 15.3507 15.211 15.281 15.2807C15.2114 15.3504 15.1286 15.4056 15.0376 15.4433C14.9465 15.4811 14.849 15.5005 14.7504 15.5005C14.6519 15.5005 14.5543 15.4811 14.4632 15.4433C14.3722 15.4056 14.2895 15.3504 14.2198 15.2807L8.00042 9.06036L1.78104 15.2807C1.64031 15.4214 1.44944 15.5005 1.25042 15.5005C1.05139 15.5005 0.860523 15.4214 0.719792 15.2807C0.579062 15.1399 0.5 14.9491 0.5 14.7501C0.5 14.551 0.579062 14.3602 0.719792 14.2194L6.9401 8.00005L0.719792 1.78068C0.579062 1.63995 0.5 1.44907 0.5 1.25005C0.5 1.05103 0.579062 0.860156 0.719792 0.719426C0.860523 0.578695 1.05139 0.499634 1.25042 0.499634C1.44944 0.499634 1.64031 0.578695 1.78104 0.719426L8.00042 6.93974L14.2198 0.719426C14.3605 0.578695 14.5514 0.499634 14.7504 0.499634C14.9494 0.499634 15.1403 0.578695 15.281 0.719426C15.4218 0.860156 15.5008 1.05103 15.5008 1.25005C15.5008 1.44907 15.4218 1.63995 15.281 1.78068L9.06073 8.00005L15.281 14.2194Z", fill: "#191919" })))), h("div", { class: "personalize-title" }, this.filterHeader), h("div", { class: "container-body" }, this.filters.map(group => (h("div", { class: "mobile-section", key: group.id }, h("div", { class: "mobile-section-header" }, h("div", { class: "mobile-section-label" }, group.label)), h("div", { class: "chip-row" }, group.options.map(option => (h("chip-component", { key: option.value, label: option.label, colors: option.swatch ? option.swatch.join(',') : undefined, selectable: true, selected: !!(this.selectedFilters[group.id]?.includes(option.value)), onChipSelect: () => this.handleOptionCheck(group.id, option.value), onKeyDown: e => this.handleChipKeyDown(e, group.id, option.value) })))))))), h("div", { class: "mobile-footer" }, h("div", { class: `clear-filters__mobile${this.isClearFilterDisabled() ? ' clear-filters__mobile--disabled' : ''}`, onClick: this.isClearFilterDisabled() ? undefined : this.clearFilters, "aria-disabled": this.isClearFilterDisabled() ? 'true' : 'false', tabIndex: this.isClearFilterDisabled() ? -1 : 0, onKeyDown: this.handleKeyDownClearFilters }, this.clearFilterTitle), h("div", { class: `clr-filter-btn${this.resultCount === 0 ? ' clr-filter-btn--disabled' : ''}` }, h("button-component", { onClick: () => this.commitAndClose(), label: "clear-filter-btn", isArrow: false, btndisabled: this.resultCount === 0 ? 'Yes' : 'No' }, this.resultCount === 0
                ? this.noResultFoundButtonTitle
                : this.resultButtonTemplate.replace('{count}', this.resultCount.toString()))))));
        }
        else {
            return (h("div", { class: "personalize-filter-container" }, h("div", { class: "filter-header" }, h("div", { class: "filter-caption" }, h("span", { class: "filter-icon", "aria-hidden": "true" }, h(SlidersHorizontalIcon, { className: "filter-svg" })), h("h3", { class: "filter-title" }, this.filterHeader)), h("h4", { class: `clear-filters${this.isClearFilterDisabled() ? ' clear-filters--disabled' : ''}`, onClick: this.isClearFilterDisabled() ? undefined : this.clearFilters, "aria-disabled": this.isClearFilterDisabled() ? 'true' : 'false', tabIndex: this.isClearFilterDisabled() ? -1 : 0, onKeyDown: this.handleKeyDownClearFilters }, this.clearFilterTitle)), this.filters.map((group) => {
                const chipsSelectedAndClosed = (this.openGroup !== group.id) &&
                    (this.selectedFilters[group.id]?.length ?? 0) > 0;
                // Min width of container
                const limit = this.selectedChipContainerWidth > 0 && this.selectedChipContainerWidth <= CONTAINER_WIDTH;
                const calcMaxWidth = limit ? '160px' : undefined;
                // Ensures atleast one chip is visible in small width containers
                return (h("div", { class: "filter-accordion-block", key: group.id }, h("accordion-component", { noBottomPadding: chipsSelectedAndClosed, headline: group.label, open: this.openGroup === group.id, onAccordionToggle: () => this.toggleOpenGroup(group.id) }, this.openGroup === group.id && (h("div", { class: "filter-options-list" }, h("span", { "aria-live": "polite", "aria-atomic": "true", class: "visually-hidden" }, `${this.selectedFilters[group.id]?.length} of ${group.options.length} filter selected. `), group.options.map((option, id) => (h("checkbox-component", { showBorder: id !== group.options.length - 1, checked: (this.selectedFilters[group.id] || []).includes(option.value), label: option.label, colors: option.swatch ? option.swatch.join(',') : undefined, onCheckboxChange: () => {
                        this.handleOptionCheck(group.id, option.value);
                    }, onKeyDown: e => this.handleCheckboxKeyDown(e, group.id, option.value) })))))), chipsSelectedAndClosed && (h("div", { class: "selected-chips", ref: el => this.selectedChipsEl = el }, (this.visibleChips[group.id] || []).map(sel => (h("chip-component", { ref: el => {
                        if (el)
                            this.chipRefs.set(sel, el);
                        else
                            this.chipRefs.delete(sel);
                    }, key: sel, showIcon: true, label: group.options.find(opt => opt.value === sel)?.label || sel, onChipClose: () => this.handleChipClose(group.id, sel), maxWidth: calcMaxWidth }))), (this.hiddenChipCounts[group.id] ?? 0) > 0 && (h("chip-component", { label: `+${this.hiddenChipCounts[group.id]}` }))))));
            }), h("div", { style: { visibility: 'hidden', position: 'absolute', pointerEvents: 'none', overflow: 'hidden' }, ref: el => { this.overflowChipProbeEl = el; } }, h("chip-component", { label: `+${this._currentProbeOverflowValue()}` }))));
        }
    }
};
PersonalizeFilter.style = personalizeFilterCss;

export { PersonalizeFilter as personalize_filter };
//# sourceMappingURL=personalize-filter.entry.js.map
